<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home</title>
  </head>
  <body>
    <ol>
      <% for (let i = 0; i < message.length; i++) { %>
      <li>
        <span> <%= message[i].user %> </span> says
        <a href="/details/<%= i %>">Expand message</a>
      </li>
      <% } %>
    </ol>
  </body>
</html>
